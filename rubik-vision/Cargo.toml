[package]
name = "rubik-vision"
version = "0.1.0"
edition = "2024"

[dependencies]
opencv = { version = "0.98.2", features = ["imgproc", "imgcodecs"], default-features = false }

[dev-dependencies]
workspace_root = "0.2.0"
